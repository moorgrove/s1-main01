apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: librespeed-netbird
  namespace: librespeed
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-rbd-hdd-r2