apiVersion: netbird.io/v1alpha1
kind: NetworkResource
metadata:
  name: librespeed
  namespace: librespeed
spec:
  networkRouterRef:
    name: prod
    namespace: netbird
  serviceRef:
    name: librespeed
  # groups:
  #   - name: kubernetes