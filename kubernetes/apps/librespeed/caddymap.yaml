apiVersion: v1
kind: ConfigMap
metadata:
    name: librespeed-caddyfile
    namespace: librespeed
data:
    Caddyfile: |-
        {
          auto_https off
        }
        :443 {
          tls /certs/tls.crt /certs/tls.key
          reverse_proxy localhost:80
        }
