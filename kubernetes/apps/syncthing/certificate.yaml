apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: syncthing-tls
  namespace: syncthing
spec:
  secretName: syncthing-tls
  dnsNames:
    - ${DNS_NAME_SYNCTHING}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
