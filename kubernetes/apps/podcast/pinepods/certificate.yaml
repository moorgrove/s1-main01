apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pinepods-tls
  namespace: podcast
spec:
  secretName: pinepods-tls
  dnsNames:
    - ${DNS_NAME_PINEPODS}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer