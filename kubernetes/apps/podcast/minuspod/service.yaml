---
apiVersion: v1
kind: Service
metadata:
  name: minuspod
  namespace: minuspod
  annotations:
    io.cilium/lb-ipam-ips: "192.168.201.111"
spec:
  type: LoadBalancer
  selector:
    app: minuspod
  ports:
    - port: 80
      targetPort: 8000
      name: http
      protocol: TCP