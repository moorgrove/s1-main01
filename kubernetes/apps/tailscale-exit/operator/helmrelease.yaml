---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-exit-operator
  namespace: tailscale-exit
spec:
  interval: 10m
  targetNamespace: tailscale-exit
  chart:
    spec:
      chart: tailscale-operator
      version: "1.96.5"
      sourceRef:
        kind: HelmRepository
        name: tailscale
        namespace: tailscale-exit
  values:
    installCRDs: true
    fullnameOverride: tailscale-exit-operator
  valuesFrom:
    - kind: Secret
      name: tailscale-exit-operator-oauth
      valuesKey: client-id
      targetPath: oauth.clientId
    - kind: Secret
      name: tailscale-exit-operator-oauth
      valuesKey: client-secret
      targetPath: oauth.clientSecret