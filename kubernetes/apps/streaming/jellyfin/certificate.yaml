apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyfin-tls
  namespace: streaming
spec:
  secretName: jellyfin-tls
  dnsNames:
    - ${DNS_NAME_JELLYFIN}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
