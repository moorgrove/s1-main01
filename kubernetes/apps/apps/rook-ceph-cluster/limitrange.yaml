apiVersion: v1
kind: LimitRange
metadata:
  name: rook-ceph-default-limits
  namespace: rook-ceph
spec:
  limits:
  - default:
      cpu: 300m
      memory: 256Mi
    defaultRequest:
      cpu: 100m
      memory: 128Mi
    type: Container