---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: samba-tm-claes-hedlund-pvc
  namespace: samba
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem-hdd
  resources:
    requests:
      storage: 500Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: samba-tm-asa-hedlund-pvc
  namespace: samba
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem-hdd
  resources:
    requests:
      storage: 500Gi