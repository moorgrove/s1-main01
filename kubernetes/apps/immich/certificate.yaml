apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-tls
  namespace: immich
spec:
  secretName: immich-tls
  dnsNames:
    - ${DNS_NAME_IMMICH}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer