apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: vaultwarden
spec:
  secretName: vault-tls
  dnsNames:
    - ${DNS_NAME_VAULTWARDEN}
  issuerRef:
    name: desec-issuer
    kind: ClusterIssuer