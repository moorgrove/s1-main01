apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: navidrome-tls
  namespace: navidrome
spec:
  secretName: navidrome-tls
  dnsNames:
    - ${DNS_NAME_NAVIDROME}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer