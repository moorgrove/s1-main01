apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: navidrome-tls
  namespace: music
spec:
  secretName: navidrome-tls
  dnsNames:
    - ${DNS_NAME_NAVIDROME}
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer