apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-netbird
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd-hdd-r2
  resources:
    requests:
      storage: 100Mi