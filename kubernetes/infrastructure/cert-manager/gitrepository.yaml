apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cert-manager-webhook-desec
  namespace: cert-manager
spec:
  interval: 24h
  url: https://github.com/kmorning/cert-manager-webhook-desec.git
  ref:
    branch: master