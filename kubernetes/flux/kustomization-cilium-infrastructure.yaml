---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cilium-infrastructure
  namespace: flux-system
spec:
  interval: 60m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/infrastructure/cilium
  prune: true
  wait: true
  timeout: 30m
  postBuild:
    substituteFrom:
      - kind: Secret
        name: flux-podcast-secrets