apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: rook-ceph-secrets
    namespace: flux-system
spec:
    dependsOn:
      - name: rook-ceph-infrastructure
    interval: 60m
    sourceRef:
        kind: GitRepository
        name: flux-system
    path: ./kubernetes/apps/rook-ceph/rook-ceph-secrets
    prune: false
    wait: true
    timeout: 5m
    decryption:
        provider: sops
        secretRef:
            name: sops-age
